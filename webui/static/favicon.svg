<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 64 64">
  <style>
    :root {
      --favicon-bg: #efe3c2;
      --favicon-fg: #2b261d;
    }

    rect {
      fill: var(--favicon-bg);
    }

    text {
      fill: var(--favicon-fg);
      font-family: ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
      font-size: 26px;
      font-weight: 800;
      letter-spacing: 0;
    }
  </style>
  <rect width="64" height="64" rx="12" />
  <text x="32" y="40" text-anchor="middle">CA</text>
</svg>
